spring:
  profiles:
    active:
      - dev

---
spring:
  profiles: dev #开发环境
  application:
    name: microservicecloud-config-dev


---
spring:
  profiles: test
  application:
    name: microservicecloud-config-test


#切记必须是utf吧 要不可能会乱码  用三个-来区别不同的配置环境